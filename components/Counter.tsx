import { chakra } from "@chakra-ui/react";
import { memo } from "react";

const Counter = () => {
  return <chakra.div bg="url(https://count.getloli.com/get/@phos)" />;
};

export default memo(Counter);
